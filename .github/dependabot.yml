version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    ignore:
      # zod 3 -> 4 is a deliberate, manual upgrade: the MCP SDK turns these
      # schemas into each tool's inputSchema JSON, so a major bump can change
      # the emitted schema without failing the test suite.
      - dependency-name: "zod"
        update-types: ["version-update:semver-major"]
      # Hold the TypeScript 5.x line; 6.x is a transitional major.
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
      # engines is >=22 and CI runs Node 22; do not pull @types/node ahead
      # of the supported runtime.
      - dependency-name: "@types/node"
        versions: [">=23"]
      # Hold vitest 3.x. A 4.x bump rewrote the lockfile without optional esbuild
      # platform packages, so `npm ci` failed (PR #6). Major upgrades stay manual.
      - dependency-name: "vitest"
        update-types: ["version-update:semver-major"]
